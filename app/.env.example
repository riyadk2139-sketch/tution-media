# Copy to .env.local and fill in. Vite exposes VITE_* vars to the client.
# Leave both blank to run in offline mode (localStorage only — same as the prototype).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
